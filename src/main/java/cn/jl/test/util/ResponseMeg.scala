package cn.jl.test.util

class ResponseMeg[T] (
                     var code: Int,
                     var msg: String,
                     var data: T
                     ){

    /*private var code = 0
    private var msg = ""
    private var data: T = _*/
   /* def this(code: Int, msg: String, data:T) {
        this()
        this.code = code
        this.msg = msg
        this.data = data
    }*/
}
